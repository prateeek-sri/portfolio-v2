export const metadata = {
  title: "timeline ✦ prateek",
  description: "Explore the timeline of my personal and professional education.",
};

export default function TimelineLayout({ children }) {
  return <>{children}</>;
}
